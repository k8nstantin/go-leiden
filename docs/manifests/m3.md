---
layout: default
title: M3 — Quality functions
---

[← M2 Algorithm phases](m2) · [Home](../) · [M4 Public API →](m4)

# M3 — Quality functions

Modularity and CPM quality functions with graspologic-native's corrected resolution scaling formula.

→ GitHub Issue: [#3](https://github.com/k8nstantin/go-leiden/issues/3) · [← Back to Product](../index.md)

---

## Role in the product

This is step 3 of 5 in the sequential build chain:

```
M1→ M2→ M3→ M4→ M5
        ^— you are here
```

**Depends on:** M2  
**Unblocks:** M4

---

## What the agent builds

# M3 — Quality Functions

## Objective
Implement Modularity and CPM quality functions, including the resolution scaling that feeds into the Phase 1 QVI calculation.

## Modularity
Measures how much within-community edge density exceeds random expectation.

Resolution scaling (graspologic-native corrected formula):
```
adjustedResolution = resolution / (2.0 × (totalEdgeWeight + totalSelfLinksWeight))
```
Node weight = degree (sum of incident edge weights).

## CPM (Constant Potts Model)
Measures internal density vs a threshold.

Resolution scaling: `adjustedResolution = resolution` (no scaling).
Node weight = 1.0 (uniform).

## ComputeQuality
An exported function that scores a complete partition. Used for: reporting final result quality, comparing Trials runs, and tests.

## Reference
`packages/network_partitions/src/quality.rs` and `resolution.rs` in graspologic-native.


---

## Task: T1 — Implement Modularity and CPM

The agent receives this cascading prompt — product context → manifest context → task specification:

<product>
  <name>go-leiden</name>
  <module>github.com/openpraxis/go-leiden</module>
  <description>
    A standalone, open-source Go library — the FIRST and ONLY native Go implementation of
    the Leiden community detection algorithm. Zero external dependencies. Single static binary.

    The Leiden algorithm (Traag, Waltman and van Eck, 2019) partitions graph nodes into
    communities guaranteed to be well-connected. It fixes Louvain's core flaw: Louvain can
    produce internally disconnected communities; Leiden cannot.

    This is a Go port of graspologic-native (Microsoft Research + Johns Hopkins, MIT license),
    the Rust implementation used in production by Microsoft GraphRAG. We port from
    graspologic-native — NOT from leidenalg (C++ + igraph) — because graspologic-native
    is already igraph-free, self-contained, and clean.
  </description>
  <goals>
    - Pure Go, zero CGO, zero external imports (stdlib only)
    - Published as github.com/openpraxis/go-leiden under MIT license
    - Production-quality: fully tested, benchmarked, fuzz-tested
    - All Gophers can use it — not tied to OpenPraxis
  </goals>
  <reference_implementation>
    graspologic-native (Rust): https://github.com/graspologic-org/graspologic-native
    Read the source before implementing. The Go code mirrors this Rust code.
  </reference_implementation>
  <build_order>
    M1 — Core data structures (CompactNetwork, Clustering, Edge) ← YOU ARE HERE or later
    M2 — Algorithm phases (local-move, refinement, aggregation)
    M3 — Quality functions (Modularity, CPM, resolution scaling)
    M4 — Public API (Leiden, HierarchicalLeiden, go.mod, README)
    M5 — Test suite (validation, benchmarks, fuzz)
  </build_order>
</product>

<manifest>
  <name>M3 — Quality functions</name>
  <role>
    Provides the mathematical scoring functions (Modularity and CPM) and the resolution
    scaling that feeds into the Phase1 QVI calculation in M2.
    Depends on M1 data structures. Independent of M2 algorithm phases.
  </role>
  <prerequisite>M1 is complete. network.go, clustering.go exist and tests pass.</prerequisite>
  <deliverable>quality.go + quality_test.go in package leiden</deliverable>
</manifest>

<task>
  <title>Implement Modularity and CPM quality functions</title>

  <working_directory>/tmp/go-leiden (existing module from M1)</working_directory>

  <reference_files>
    https://github.com/graspologic-org/graspologic-native/blob/main/packages/network_partitions/src/quality.rs
    https://github.com/graspologic-org/graspologic-native/blob/main/packages/network_partitions/src/resolution.rs
  </reference_files>

  <quality_functions>
    <modularity>
      Measures within-community edge density vs random expectation.
      Node weight = degree (sum of incident edge weights) — already in CompactNetwork.
      Resolution scaling (graspologic-native corrected formula):
        adjustedResolution = resolution / (2.0 × (totalEdgeWeight + totalSelfLinksWeight))
      Special case: totalEdgeWeight == 0 → adjustedResolution = 0
    </modularity>
    <cpm>
      Constant Potts Model. Measures internal density vs a threshold.
      Node weight = 1.0 (uniform — override CompactNetwork's stored weights).
      Resolution scaling: adjustedResolution = resolution (no scaling)
    </cpm>
  </quality_functions>

  <api>
    type QualityFunction int
    const ( Modularity QualityFunction = iota; CPM )

    // computeAdjustedResolution: resolution scaling per quality function (unexported)
    func computeAdjustedResolution(network *CompactNetwork, resolution float64, qf QualityFunction) float64

    // nodeWeightsFor: returns appropriate node weight slice (unexported)
    // Modularity: degree weights from CompactNetwork
    // CPM: all-1.0 slice (allocate fresh)
    func nodeWeightsFor(network *CompactNetwork, qf QualityFunction) []float64

    // ComputeQuality: exported — scores a full partition. Used in public API and tests.
    func ComputeQuality(network *CompactNetwork, clustering *Clustering, resolution float64, qf QualityFunction) float64
  </api>

  <constraints>
    - ComputeQuality output must match graspologic-native within 1e-9 tolerance.
    - No external imports.
    - go test ./... and go vet ./... must pass.
  </constraints>
</task>

---

## Reference files
- [quality.rs](https://github.com/graspologic-org/graspologic-native/blob/main/packages/network_partitions/src/quality.rs)\n- [resolution.rs](https://github.com/graspologic-org/graspologic-native/blob/main/packages/network_partitions/src/resolution.rs)

---

[← M2 Algorithm phases](m2) · [Home](../) · [M4 Public API →](m4)
