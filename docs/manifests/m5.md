---
title: "M5: Tests, benchmarks, fuzz"
---

[← M4](m4.md) · [← Product](../index.md)

# M5 — Tests, benchmarks, fuzz

The publication gate. Validates correctness against the **karate club graph** (classic 34-node benchmark, modularity > 0.35), two planted partitions (deterministic, seed=42), and quality score consistency. Benchmarks at 100 / 1k / 10k nodes (10k must complete under 5 seconds). Fuzz testing must not panic after 30 seconds.

→ GitHub Issue: [#5](https://github.com/k8nstantin/go-leiden/issues/5)

---

## Where this fits

```
M1: data structures  →  M2: algorithm  →  M3: quality  →  M4: public API  →  M5: tests
        ^
```

**Depends on:** M4  
**Unblocks:** publication to pkg.go.dev

---

## Manifest prompt

*This is what the agent reads to understand the component's role and objective:*

```
# M5 — Test Suite, Benchmarks, Validation

## Objective
Write the test suite that validates correctness against known outputs, benchmarks performance, and fuzzes the public API for panics.

## Validation tests
- **Karate club graph** (Zachary 1977, 34 nodes, 78 edges): classic benchmark. At resolution=1.0, modularity should be > 0.35.
- **Two planted partitions** (2 × 10-node cliques, weak bridge): should produce exactly 2 communities. Deterministic with seed=42.
- **Quality score reproducibility**: ComputeQuality(result.Partition) == result.Quality within 1e-9.
- **HierarchicalLeiden max cluster size**: all final clusters ≤ MaxClusterSize.

## Benchmarks
- 100 nodes, 1k nodes, 10k nodes
- 10k-node graph must complete in under 5 seconds
- Report allocs/op

## Fuzz
- `FuzzLeiden`: arbitrary byte input → must never panic
- Specifically targets weight cache invariants in localMovePhase

## Done
`go test ./...` passes, `go test -bench=. shows sub-5s at 10k nodes`, `go test -fuzz=FuzzLeiden -fuzztime=30s` completes without panic.
```

---

## Task: T1 — Write test suite, benchmarks, fuzz tests

*This is the full cascading prompt the agent executes — product context → manifest context → exact implementation spec:*

```
<task>
  <title>M5/T1 — go-leiden: Test suite, benchmarks, validation against graspologic-native</title>
  <prerequisite>M4/T1 complete. Full public API exists and all unit tests pass.</prerequisite>
  <repository>Existing go-leiden module at /tmp/go-leiden</repository>
</task>

<reference>
  graspologic-native test suite (correctness target):
  - packages/network_partitions/tests/  — Rust integration tests
  - The Python graspologic test suite: graspologic/tests/test_leiden.py

  These tests define the expected behaviour. Our results must match within floating-point
  tolerance (1e-9 for quality scores).

  NOTE: You cannot run graspologic-native directly. Instead:
  1. Manually compute expected outputs for small graphs using the algorithm specification.
  2. Use the known test vectors from the graspologic test file (hardcoded expected values).
  3. Cross-check quality scores using ComputeQuality independently.
</reference>

<deliverables>
  <file path="validation_test.go">
    // Validation tests against known graspologic-native outputs.
    // These are regression tests — if any fail, the implementation has diverged.

    // Test vector 1: Karate club graph (Zachary 1977)
    // Classic benchmark. 34 nodes, 78 edges. Two known communities.
    // The Leiden algorithm at resolution=1.0 should produce >= 2 communities
    // with modularity > 0.35 (graspologic-native verified).
    // Embed the karate club edge list as a package-level var.

    // Test vector 2: Two planted partitions
    // 20 nodes split into two groups of 10.
    // Within-group: all pairs connected with weight 1.0
    // Between-group: two bridge edges with weight 0.1
    // Expected: exactly 2 communities, each containing all 10 within-group nodes.
    // Resolution = 1.0, seed = 42.

    // Test vector 3: Quality score reproducibility
    // For both test vectors above: ComputeQuality(result.Partition) must equal result.Quality
    // within 1e-9 tolerance.

    // Test vector 4: HierarchicalLeiden on planted partitions
    // 40 nodes, 4 groups of 10, MaxClusterSize = 15.
    // All HierarchicalCluster.IsFinal == true nodes must be in clusters of size <= 15.
  </file>

  <file path="bench_test.go">
    // Benchmarks. Run with: go test -bench=. -benchmem ./...

    // BenchmarkLeiden_100nodes:    random graph, 100 nodes, ~300 edges
    // BenchmarkLeiden_1000nodes:   random graph, 1000 nodes, ~5000 edges
    // BenchmarkLeiden_10000nodes:  random graph, 10000 nodes, ~50000 edges
    //
    // For each: report ns/op and allocs/op.
    // Target: 10000-node graph should complete in under 5 seconds on a modern laptop.
    //
    // Random graph generation: use math/rand with fixed seed for reproducibility.
    // Use Erdos-Renyi model: each possible edge included with probability p=0.001 * (1/sqrt(n))
    // to keep edge count proportional to n rather than n^2.
    //
    // BenchmarkNewCompactNetwork_1000edges:
    //   Benchmark graph construction separately from clustering.
    //
    // BenchmarkComputeQuality_1000nodes:
    //   Benchmark quality scoring separately.
  </file>

  <file path="fuzz_test.go">
    // Fuzz tests for robustness. Run with: go test -fuzz=FuzzLeiden -fuzztime=60s
    //
    // FuzzLeiden: feed arbitrary []byte as edge data.
    //   Convert fuzzer input to edges: pairs of bytes as node IDs, weight from byte value.
    //   Must not panic. Must return either a valid Result or a documented error.
    //   No invalid memory access, no infinite loops.
    //
    // This specifically targets the weight cache invariants in localMovePhase.
  </file>
</deliverables>

<constraints>
  <constraint>All validation tests must pass with go test ./... -count=1.</constraint>
  <constraint>Benchmarks must compile and run without errors.</constraint>
  <constraint>Fuzz test must not panic on any input after 60 seconds of fuzzing.</constraint>
  <constraint>No test may depend on an external network connection or external binary.</constraint>
  <constraint>The karate club edge list must be embedded as a Go slice literal, not read from a file.</constraint>
</constraints>

<done_criteria>
  - go test ./... -v passes all tests including validation vectors
  - go test -bench=. shows 10k-node benchmark under 5s
  - go test -fuzz=FuzzLeiden -fuzztime=30s completes without panic
  - go vet ./... clean
  - The library is ready to publish as github.com/openpraxis/go-leiden
</done_criteria>
```

---

[← M4](m4.md) · [← Product](../index.md)
